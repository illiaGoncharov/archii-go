# Быстрый старт Archi-Go

## Шаги для запуска MVP за 10 минут

### 1. Установи зависимости
```bash
npm install
```

### 2. Настрой Firebase (5 минут)

1. Зайди на [Firebase Console](https://console.firebase.google.com/)
2. Создай проект
3. Включи:
   - **Authentication** → Email/Password
   - **Firestore** → Test mode
   - **Storage** → Test mode
4. Скопируй конфигурацию из настроек проекта
5. Вставь в `firebase-config.ts`

### 3. Заполни базу данных
```bash
npx ts-node scripts/seed-data.ts
```

Это добавит:
- 15 архитектурных элементов
- 5 точек интереса в Петербурге

### 4. Запусти приложение
```bash
npm start
```

### 5. Тестируй на iPhone

1. Установи **Expo Go** из App Store
2. Отсканируй QR-код
3. Готово! 🎉

## Что дальше?

### Для MVP тестирования:
1. Зарегистрируй аккаунт в приложении
2. Открой карту — увидишь 5 POI в Петербурге
3. Открой камеру — сделай снимок
4. Открой коллекцию — увидишь 15 элементов (пока с замочком)
5. Открой профиль — статистика и конструктор

### Модерация находки (вручную):
1. После съёмки фото создаётся запись в Firebase
2. Зайди в Firebase Console → Firestore → `findings`
3. Найди запись со статусом `pending`
4. Измени:
   - `status` → `approved`
   - `elementId` → ID элемента из коллекции `elements`
5. Элемент появится в коллекции пользователя

### Для продакшен-готового MVP:
- Замени placeholder изображения на реальные фото элементов
- Добавь больше POI в твоём городе
- Настрой правила безопасности в Firebase (см. SETUP.md)
- Создай админку для модерации находок (опционально)

## Полезные команды

```bash
# Запуск
npm start

# Только iOS
npm run ios

# Очистка кэша
npx expo start --clear

# Сборка для продакшена
npx eas build --platform ios
```

## Проблемы?

См. раздел "Частые проблемы" в `SETUP.md`

