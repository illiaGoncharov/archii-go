# Checklist для запуска Archi-Go MVP

Следуй этому чеклисту шаг за шагом, чтобы запустить приложение.

## ☐ Шаг 1: Установка зависимостей

```bash
cd /Users/illia/Documents/_2025/_projects/_pets/archi-go
npm install
```

**Ожидаемое время:** 2-3 минуты

---

## ☐ Шаг 2: Настройка Firebase

### 2.1. Создай проект в Firebase Console

1. Зайди на https://console.firebase.google.com/
2. Нажми "Add project" (Создать проект)
3. Имя: **archi-go-mvp** (или любое другое)
4. Отключи Google Analytics (пока не нужен)
5. Нажми "Create project"

### 2.2. Включи Authentication

1. В меню слева: **Build** → **Authentication**
2. Нажми "Get started"
3. Выбери **Email/Password**
4. Включи (toggle ON)
5. Сохрани

### 2.3. Создай Firestore Database

1. В меню слева: **Build** → **Firestore Database**
2. Нажми "Create database"
3. Выбери **Start in test mode**
4. Регион: **europe-west1** (или ближайший)
5. Нажми "Enable"

### 2.4. Создай Storage

1. В меню слева: **Build** → **Storage**
2. Нажми "Get started"
3. Выбери **Start in test mode**
4. Нажми "Done"

### 2.5. Получи конфигурацию

1. Нажми на шестерёнку ⚙️ → **Project settings**
2. Прокрути вниз до **Your apps**
3. Нажми на **Web app** (</> иконка)
4. Nickname: **archi-go-web**
5. Не включай Firebase Hosting
6. Нажми "Register app"
7. **Скопируй объект firebaseConfig**

### 2.6. Вставь конфигурацию в проект

Открой файл `firebase-config.ts` и замени:

```typescript
export const firebaseConfig = {
  apiKey: "ТВОЙ_API_KEY",
  authDomain: "твой-проект.firebaseapp.com",
  projectId: "твой-проект-id",
  storageBucket: "твой-проект.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```

**Ожидаемое время:** 10 минут

---

## ☐ Шаг 3: Заполнение базы данных

```bash
npx ts-node scripts/seed-data.ts
```

Должен увидеть:
```
🌱 Начинаем заполнение базы данных...
📦 Добавляем архитектурные элементы...
  ✓ Метопа
  ✓ Сандрик
  ... (ещё 13 элементов)
📍 Добавляем точки интереса...
  ✓ Домик Петра I
  ... (ещё 4 POI)
✅ База данных успешно заполнена!
```

**Ожидаемое время:** 1 минута

---

## ☐ Шаг 4: Запуск приложения

```bash
npm start
```

Должен увидеть:
```
› Metro waiting on exp://192.168.x.x:8081
› Scan the QR code above with Expo Go
```

**Не закрывай это окно!**

---

## ☐ Шаг 5: Установка Expo Go на iPhone

1. Открой App Store на iPhone
2. Найди **Expo Go**
3. Установи (бесплатно)

---

## ☐ Шаг 6: Запуск на iPhone

1. Открой **Камеру** на iPhone
2. Наведи на QR-код в терминале
3. Нажми на уведомление "Open in Expo Go"
4. Приложение откроется

**Первый запуск может занять 1-2 минуты**

---

## ☐ Шаг 7: Разрешения

При первом запуске разреши:
- ✅ Доступ к камере
- ✅ Доступ к геолокации

---

## ☐ Шаг 8: Регистрация тестового пользователя

1. В приложении зарегистрируйся:
   - Email: `test@test.com`
   - Пароль: `test123` (минимум 6 символов)
   - Имя: `Тестер`

**Примечание:** Пока нет экрана регистрации, нужно сделать это через Firebase Console:

### Регистрация через Firebase Console:

1. Firebase Console → **Authentication** → **Users**
2. Нажми "Add user"
3. Email: `test@test.com`
4. Password: `test123`
5. Нажми "Add user"
6. Скопируй **User UID**
7. Зайди в **Firestore Database**
8. Коллекция `users` → "Add document"
9. Document ID: вставь **User UID**
10. Поля:
    - `email` (string): test@test.com
    - `displayName` (string): Тестер
    - `level` (number): 1
    - `totalPoints` (number): 0
    - `createdAt` (string): 2025-11-02T00:00:00Z

---

## ☐ Шаг 9: Проверка функциональности

### Карта
- [ ] Открывается карта
- [ ] Видна моя геолокация (синяя точка)
- [ ] Видны 5 POI (цветные маркеры)
- [ ] При клике на маркер открывается информация

### Камера
- [ ] Открывается камера
- [ ] Можно сделать снимок
- [ ] После снимка показывается превью
- [ ] Кнопки "Переснять" и "Отправить" работают
- [ ] После отправки показывается "Фотография отправлена на модерацию"

### Коллекция
- [ ] Показывает 15 элементов в сетке
- [ ] Все элементы с замочком (пока не найдены)
- [ ] При клике открывается детальная информация

### Профиль
- [ ] Показывает имя пользователя
- [ ] Показывает уровень 1, 0 очков
- [ ] Показывает "0 найдено элементов"
- [ ] Кнопка "Конструктор фасадов" работает

### Конструктор
- [ ] Открывается пустой холст
- [ ] Кнопка "Добавить элемент" открывает палитру
- [ ] В палитре пусто (ещё нет найденных элементов)

---

## ☐ Шаг 10: Тестовая находка (модерация вручную)

### 10.1. Создай находку через камеру
1. Открой камеру
2. Сделай снимок
3. Отправь

### 10.2. Модерируй через Firebase Console
1. Firebase Console → **Firestore** → `findings`
2. Найди запись со статусом `pending`
3. Открой её
4. Измени поля:
   - `status`: `approved`
   - `elementId`: скопируй ID любого элемента из коллекции `elements` (например, ID метопы)
5. Сохрани

### 10.3. Проверь в приложении
1. Перезагрузи приложение (pull to refresh или перезапусти)
2. Открой коллекцию
3. Элемент должен быть разблокирован (цветной)
4. Открой профиль — должно показать +10 очков

---

## ☐ Шаг 11: Прогулка с приложением (опционально)

Если ты в Петербурге:
1. Открой карту
2. Найди ближайший POI
3. Иди туда
4. Ищи архитектурные элементы
5. Фотографируй

Если не в Петербурге:
- Добавь свои POI в базу (см. CONTRIBUTING.md)

---

## ✅ Готово!

Приложение полностью работает и готово к тестированию.

### Следующие шаги:

1. **Замени placeholder изображения на реальные фото**
   - Сфотографируй элементы
   - Загрузи в Firebase Storage
   - Обнови `imageUrl` в Firestore

2. **Пройди прогулку с приложением**
   - Протестируй все функции
   - Собери фидбек

3. **Добавь больше элементов и POI**
   - Расширь базу данных
   - Добавь свои локации

---

## 🆘 Проблемы?

Если что-то не работает:

### Приложение не запускается
```bash
# Очисти кэш
npx expo start --clear
```

### Firestore пустой
```bash
# Запусти seed-data снова
npx ts-node scripts/seed-data.ts
```

### Камера не работает
- Камера работает только на реальном устройстве (не в симуляторе)
- Проверь разрешения в настройках iPhone

### Карта не показывает геолокацию
- Проверь разрешения в настройках iPhone → Expo Go → Геолокация

### Элементы не загружаются
- Проверь Firebase Console → Firestore
- Убедись, что коллекция `elements` не пустая

---

**Если проблема не решается — пиши!**

