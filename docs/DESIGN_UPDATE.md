# Обновление дизайна: GitHub-стиль + IBM Plex

## ✅ Выполнено

### 1. Обновлена философия дизайна

**Было:** Минималистичный, игровой  
**Стало:** GitHub-стиль — творческий, но структурный, не отвлекающий

**Принципы:**
- Структурность (как GitHub, Linear)
- Творческий минимализм
- IBM Plex типографика
- Фокус на контенте (архитектурных элементах)

### 2. Цветовая палитра — GitHub Primer

**Обновлённые цвета:**

| Было | Стало | Описание |
|------|-------|----------|
| `#3B82F6` | `#0969DA` | Primary — GitHub blue |
| `#F9FAFB` | `#F6F8FA` | Background — GitHub gray |
| `#111827` | `#24292F` | Text Primary — GitHub fg-default |
| `#6B7280` | `#57606A` | Text Secondary — GitHub fg-muted |
| `#10B981` | `#1A7F37` | Success — GitHub green |
| `#EF4444` | `#CF222E` | Error — GitHub red |

**Новые цвета редкости** (приглушённые, но выразительные):
- Common: `#6E7781` (серый)
- Rare: `#0969DA` (синий)
- Epic: `#8250DF` (фиолетовый)
- Legendary: `#BF8700` (золотой)

**Новые акценты для архитектурных стилей:**
- Барокко: `#8250DF` (фиолетовый)
- Классицизм: `#0969DA` (синий)
- Модерн: `#1A7F37` (зелёный)
- Ампир: `#BF8700` (золотой)

### 3. Типографика — IBM Plex

**Добавлены зависимости:**
```json
"expo-font": "~12.0.0",
"@expo-google-fonts/ibm-plex-sans": "^0.2.3",
"@expo-google-fonts/ibm-plex-mono": "^0.2.3"
```

**Шрифты:**
- `IBMPlexSans-Regular` — основной
- `IBMPlexSans-Medium` — средний
- `IBMPlexSans-SemiBold` — жирный
- `IBMPlexMono-Regular` — моноширинный (для кода, цифр)

**Размеры (как на GitHub):**
- Body: `14px` (было `16px`)
- Button: `14px` (было `16px`)
- Caption: `12px` (было `14px`)

**Line-height:** меньше для плотности (как GitHub)

### 4. Border Radius

**Обновлено под GitHub:**
- base: `6px` (было `8px`) — кнопки, inputs
- Остальные значения скромнее

### 5. Обновлённые файлы

- `docs/DESIGN_GUIDE.md` — полностью переписан
- `constants/design.ts` — обновлены все константы
- `package.json` — добавлены шрифты IBM Plex

---

## 🎨 Как использовать

### 1. Установи зависимости

```bash
npm install
```

### 2. Используй константы

```typescript
import { Colors, Typography, BorderRadius } from '../constants/design';

const styles = StyleSheet.create({
  container: {
    backgroundColor: Colors.surface,
    padding: Spacing.base,
    borderRadius: BorderRadius.md,
  },
  title: {
    ...Typography.h2,
    color: Colors.textPrimary,
  },
  button: {
    backgroundColor: Colors.primary,
    borderRadius: BorderRadius.base,
  },
});
```

### 3. Загрузи шрифты (в app/_layout.tsx)

```typescript
import { useFonts } from 'expo-font';
import {
  IBMPlexSans_400Regular,
  IBMPlexSans_500Medium,
  IBMPlexSans_600SemiBold,
} from '@expo-google-fonts/ibm-plex-sans';
import {
  IBMPlexMono_400Regular,
} from '@expo-google-fonts/ibm-plex-mono';

export default function RootLayout() {
  const [fontsLoaded] = useFonts({
    IBMPlexSans_400Regular,
    IBMPlexSans_500Medium,
    IBMPlexSans_600SemiBold,
    IBMPlexMono_400Regular,
  });

  if (!fontsLoaded) {
    return null; // Или SplashScreen
  }

  // ...
}
```

---

## 📐 Референсы

**Вдохновение:**
- [GitHub Primer Design System](https://primer.style/)
- [IBM Plex](https://www.ibm.com/plex/)
- [Linear](https://linear.app/) — чистота и структурность

**Почему GitHub-стиль подходит:**
- ✅ Структурный, но не скучный
- ✅ Фокус на контенте (код/архитектура)
- ✅ Читабельность (IBM Plex)
- ✅ Респектабельный (серьёзное отношение к теме)
- ✅ Не отвлекает от визуалов элементов

---

## 🔄 Миграция

Все компоненты нужно обновить:

1. Заменить старые цвета на `Colors.*`
2. Обновить fontSize (14px для body)
3. Добавить `fontFamily: Typography.fontFamily.sans`
4. Обновить borderRadius

**Пример миграции:**

**Было:**
```typescript
const styles = StyleSheet.create({
  text: {
    fontSize: 16,
    color: '#111827',
  },
  button: {
    backgroundColor: '#3B82F6',
    borderRadius: 8,
  },
});
```

**Стало:**
```typescript
const styles = StyleSheet.create({
  text: {
    ...Typography.body,
    color: Colors.textPrimary,
  },
  button: {
    backgroundColor: Colors.primary,
    borderRadius: BorderRadius.base,
  },
});
```

---

## ✨ Что получили

### Было
- Яркие цвета
- Крупный текст (16px)
- Игровой стиль
- Системные шрифты

### Стало
- Приглушённые, но выразительные цвета (GitHub Primer)
- Плотный текст (14px, как GitHub)
- Структурный, но творческий
- IBM Plex (читабельность + техничность)

### Итог
**Дизайн теперь:**
- Не отвлекает от архитектурных элементов
- Респектабельный и серьёзный
- Структурный (как IT-инструмент)
- Творческий в деталях (цвета редкости, стили)

---

**Дизайн-система обновлена! 🎨**

См. полный гайд: `docs/DESIGN_GUIDE.md`

